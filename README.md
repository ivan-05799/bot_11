# 🤖 Telegram Bot для сбора API-ключей клиентов

Telegram бот для автоматического сбора и хранения API-ключей клиентов с интеграцией вебхуков для уведомлений.

## ✨ Возможности

- 🔑 Автоматический сбор API-ключей от клиентов
- 💾 Безопасное хранение в PostgreSQL базе данных
- 🔔 Поддержка вебхуков для внешних уведомлений
- 🚀 Готов к продакшен развертыванию
- 📊 Мониторинг работоспособности
- ⚡ Интеграция с Trigger.dev

---

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка окружения

Запустите интерактивную настройку:

```bash
npm run setup
```

Скрипт автоматически:
- ✅ Проверит валидность токена бота (запрос к Telegram API)
- ✅ Проверит подключение к базе данных
- ✅ Создаст необходимые таблицы
- ✅ Сохранит настройки в `.env` файл

**Или настройте вручную:**

Создайте файл `.env`:

```env
# Токен бота (получите у @BotFather)
BOT_TOKEN=ваш_токен_бота_telegram

# Строка подключения к PostgreSQL (Neon DB)
DATABASE_URL=postgresql://user:password@host:port/database?sslmode=require
```

### 3. Запуск

```bash
# Режим разработки (с автоперезагрузкой)
npm run dev

# Продакшен
npm run build
npm start
```

---

## 📦 Развертывание на Render

### Шаги развертывания:

1. **Подключите GitHub репозиторий** к Render
2. **Создайте Web Service** в панели Render
3. **Установите переменные окружения:**
   - `BOT_TOKEN` - Токен вашего Telegram бота
   - `DATABASE_URL` - Строка подключения к PostgreSQL

4. **Настройки сборки:**
   - Build Command: `npm install && npm run build`
   - Start Command: `npm start`

5. **Автодеплой** при пуше в git активируется автоматически

---

## 🔌 API Эндпоинты

### Проверка здоровья

```http
GET /health
```

**Ответ:**
```json
{
  "status": "ok",
  "bot": "running"
}
```

### Отправка уведомления (Вебхук)

```http
POST /api/send-message
Content-Type: application/json
```

**Тело запроса:**
```json
{
  "chatId": 123456789,
  "message": "Текст вашего уведомления"
}
```

**Пример с curl:**
```bash
curl -X POST https://your-bot.onrender.com/api/send-message \
  -H "Content-Type: application/json" \
  -d '{"chatId": 123456789, "message": "Анализ завершен!"}'
```

---

## 🗄️ Структура базы данных

### Таблица `telegram_users`

```sql
CREATE TABLE telegram_users (
  id SERIAL PRIMARY KEY,
  chat_id BIGINT UNIQUE NOT NULL,
  username TEXT,
  created_at TIMESTAMP DEFAULT NOW()
);
```

### Таблица `api_keys`

```sql
CREATE TABLE api_keys (
  id SERIAL PRIMARY KEY,
  chat_id BIGINT NOT NULL,
  api_key TEXT NOT NULL,
  platform VARCHAR(50) DEFAULT 'unknown',
  account_name VARCHAR(255),
  created_at TIMESTAMP DEFAULT NOW(),
  status VARCHAR(50) DEFAULT 'pending'
);
```

---

## 📖 Руководство по интеграции

### Для конечных клиентов

1. Поделитесь ссылкой на бота: `t.me/YourBotName`
2. Клиенты отправляют `/start` для регистрации
3. Клиенты отправляют API-ключи напрямую боту
4. Ключи автоматически сохраняются в базу данных

### Для интеграции с вашей системой

#### JavaScript/Node.js

```javascript
// Отправка уведомлений конкретному пользователю
const response = await fetch('https://your-bot.onrender.com/api/send-message', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    chatId: 123456789,
    message: '⚠️ *Кампания X убыточна*\nСоветую остановить!'
  })
});

const result = await response.json();
console.log(result);
```

#### TypeScript (из вашего кода)

```typescript
import { sendAlertToAll, sendAlertToUser } from './src/notifier';

// Отправить всем подписчикам
await sendAlertToAll('✅ Анализ завершен успешно!');

// Отправить конкретному пользователю
await sendAlertToUser(123456789, '📊 Новый отчет готов!');
```

### Интеграция с Trigger.dev

```typescript
import { client } from "@trigger.dev/sdk";
import { sendAlertToAll } from "./src/notifier";

client.defineJob({
  id: "analytics-alert",
  name: "Analytics Alert",
  version: "1.0.0",
  trigger: eventTrigger({
    name: "campaign.unprofitable"
  }),
  run: async (payload, io) => {
    // Получаем данные кампании
    const campaign = payload.campaign;
    
    // Отправляем уведомление всем подписчикам
    await sendAlertToAll(
      `⚠️ *Кампания "${campaign.name}" убыточна*\n\n` +
      `ROI: ${campaign.roi}%\n` +
      `Расход: ${campaign.spend}₽\n` +
      `Советую остановить!`
    );
    
    await io.logger.info("Alert sent to all subscribers");
  }
});
```

---

## 🛠️ Использование

### Команды бота

- `/start` - Регистрация пользователя и подписка на уведомления

### Отправка уведомлений из кода

```typescript
import { sendAlertToAll, sendAlertToUser } from './src/notifier';

// Пример 1: Уведомление всем
await sendAlertToAll('⚠️ *Важное уведомление*\nСистема обновлена!');

// Пример 2: Уведомление конкретному пользователю
await sendAlertToUser(123456789, '✅ Ваш отчет готов!');

// Пример 3: С форматированием Markdown
await sendAlertToAll(
  `📊 *Отчет за ${new Date().toLocaleDateString()}*\n\n` +
  `• Кампаний: 15\n` +
  `• ROI: 250%\n` +
  `• Прибыль: 50,000₽`
);
```

---

## 📁 Структура проекта

```
telegram-bot/
├── src/
│   ├── bot.ts          # Основной бот (Long Polling)
│   ├── notifier.ts     # Функции отправки уведомлений
│   └── setup.ts        # Интерактивная настройка
├── .env                # Переменные окружения (создается автоматически)
├── .env.example        # Пример переменных окружения
├── package.json        # Зависимости и скрипты
├── tsconfig.json       # Конфигурация TypeScript
├── schema.sql          # SQL схема базы данных
└── README.md           # Документация
```

---

## 🔧 Поддержка и отладка

### При проблемах с развертыванием:

1. **Проверьте логи** в панели Render
2. **Убедитесь в корректности** переменных окружения
3. **Проверьте подключение** к базе данных
4. **Проверьте статус бота:** `https://your-bot.onrender.com/health`

### Частые проблемы:

**Бот не отвечает:**
- Проверьте валидность `BOT_TOKEN`
- Убедитесь, что бот запущен: `npm run dev`

**Ошибка подключения к БД:**
- Проверьте `DATABASE_URL`
- Убедитесь, что таблицы созданы: `npm run setup`

**Уведомления не приходят:**
- Проверьте, что пользователь отправил `/start` боту
- Проверьте `chat_id` в базе данных

---

## 📝 Скрипты

```bash
npm run setup    # Интерактивная настройка бота
npm run dev      # Запуск в режиме разработки
npm run build    # Сборка для продакшена
npm start        # Запуск продакшен версии
```

---

## 📄 Лицензия

ISC

---

## 🤝 Поддержка

Если у вас возникли вопросы или проблемы, создайте Issue в репозитории.
